name: Build .NET

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Download dependencies
        run: sudo bash packages.sh
      - name: Clone dotnet/runtime
        run: sudo bash clonerepo.sh
      - name: Generate ROOTFS
        run: sudo bash makerootfs.sh
      - name: Build CoreCLR
        run: sudo bash buildclr.sh
      - name: Build libraries
        run: sudo bash makerootfs.sh
